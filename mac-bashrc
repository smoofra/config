
if which gls >/dev/null ; then
	alias ls='gls --color=auto'
fi

if which grm >/dev/null ; then
	alias rm=grm
fi

[[ -e ~/bin/addpath ]] && PATH=$(~/bin/addpath "$HOME/bin" "$PATH" /usr/local/sbin )

export PS1='\u@\h:\w$ '

alias rerc='. ~/.bashrc'
